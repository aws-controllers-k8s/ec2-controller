apiVersion: ec2.services.k8s.aws/v1alpha1
kind: DHCPOptions
metadata:
  name: $DHCP_OPTIONS_NAME
spec:
  dhcpConfigurations:
    - key: $DHCP-KEY-1
      values:
        - $DHCP-VAL-1
    - key: $DHCP-KEY-2
      values:
        - $DHCP-VAL-2-1
        - $DHCP-VAL-2-2
